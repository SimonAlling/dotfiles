GORC=".gorc"

export EDITOR="vim"

# Aliases
alias ..="cd .."
alias g="git"
alias l='ls -CF'
alias ll="ls -al"
alias sv="sudo vim"
alias v="vim"
alias k="kubectl"
alias h="helm"

# Propagate aliases to sudo
alias sudo="sudo "

# Go
if [[ -f "$HOME/$GORC" ]]; then
    . "$HOME/$GORC"
fi
